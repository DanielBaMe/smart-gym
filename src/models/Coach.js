class Coach {
  constructor(
    id,
    nombre,
    biografia,
    email,
    horarios
  ) {
    this.id        = id
    this.nombre    = nombre
    this.biografia = biografia
    this.email     = email
    this.horarios  = horarios
  }
}

export default Coach
