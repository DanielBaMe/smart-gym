class Coach {
  constructor(
    nombre,
    biografia,
    email,
    horarios
  ) {
    this.nombre    = nombre
    this.biografia = biografia
    this.email     = email
    this.horarios  = horarios
  }
}

export default Coach
